require 'rails_helper'

RSpec.describe LlmStreamJob, type: :job do
  it 'perform_now does not raise error with required parameters' do
    expect {
      described_class.perform_now(
        stream_name: 'test_channel',
        prompt: 'Test prompt',
        system: 'Test system message'
      )
    }.not_to raise_error
  end
end
